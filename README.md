# Animating SVG path in React with styled-components

No GSAP, just css-in-js.

Import large SVG file as ReactComponent 
``` javascript
import { ReactComponent as importedLine } from "./ornament.svg";
```

![screenshot](screenshot_sm.gif)
